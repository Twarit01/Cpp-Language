#include <iostream>
using namespace std;
int main(){
    float dividend,divisor;
    cout<<"Enter dividend and divisor";
    cin>>dividend>>divisor;
    float x = dividend/divisor;
    cout<<x;
    return 0;
}
// try block:- Codes which could give errors // only 1 try block should be their in code
// throw : error throw
// catch block :- What do we need to do after throw // multiple catch blocks can be 
   